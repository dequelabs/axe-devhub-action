interface AnalyzeParams {
    __UserRequestedAnalyze?: boolean;
}
declare abstract class Controller {
    protected abstract executeScript(fn: string | ((...args: unknown[]) => Promise<void>), ...args: unknown[]): Promise<void>;
    protected isStopped: boolean;
    start(): Promise<void>;
    stop(): Promise<void>;
    analyze({ __UserRequestedAnalyze }?: AnalyzeParams): Promise<void>;
    /** Flush all axe-core results by sending them to the server. */
    flush(): Promise<void>;
}
export default Controller;
